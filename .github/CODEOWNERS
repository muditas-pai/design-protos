# Code owners — auto-requests the owner's review on PRs touching these paths.
# (Soft: requests review. To *require* it, protect `main` with "Require review
#  from Code Owners" — but that forces all changes through PRs, including
#  explorations, so we keep it review-request-only.)

# Key screens — one owner each (hand-maintained, no auto-sync)
/key-screens/editor.html       @muditas-pai
/key-screens/login.html        @muditas-pai
/key-screens/dashboard.html    @tyo-pai
/key-screens/pricing.html      @manivasakan-arch

# Shared design system — steward review
/design-system/                @muditas-pai

# Note: explorations/** intentionally have NO owner — they're free to commit.
